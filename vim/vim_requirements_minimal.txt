# Minimal requirements for VisionMamba (Compute Canada compatible)
# Only essential packages to avoid dependency conflicts

# Core dependencies
einops==0.7.0
timm==0.4.12
transformers==4.35.2
triton>=2.1.0

# Data processing
numpy==1.26.2
Pillow==10.1.0
opencv-python==4.8.1.78

# Utilities
tqdm==4.66.1
PyYAML==6.0.1
requests==2.31.0

# Optional but recommended
matplotlib==3.8.2
scikit-learn==1.3.2
scipy==1.11.4

# Note: PyTorch, mamba-ssm, and causal-conv1d should be installed separately
# Note: Many packages are already available in Compute Canada environment


